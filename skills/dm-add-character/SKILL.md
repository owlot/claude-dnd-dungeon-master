---
description: Add a single character to an existing campaign. Delegates to the character-creation sub-agent.
argument-hint: "[character name] for campaign [campaign name]"
---

Add character $ARGUMENTS

Delegate to the `character-creation` sub-agent to add a single character to an existing campaign.
